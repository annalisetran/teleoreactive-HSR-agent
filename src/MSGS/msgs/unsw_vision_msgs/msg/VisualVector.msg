# A vector msg to represent a vector for an agent to operator with objects.
# It can be from head(where it is pointing to, or point detection)
# right now use map frame but this should be able to be transformed from any frame to any frame.

geometry_msgs/Point origin
geometry_msgs/Vector3 direction
